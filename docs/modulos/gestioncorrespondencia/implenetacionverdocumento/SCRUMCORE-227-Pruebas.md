# SCRUMCORE-227 - Pruebas

## Unit tests

- `src/modules/gestionCorrespondencia/tests/DocumentosWorkbench.test.tsx`
  - Verifica que `row_click` y `menu_action ver_documento` convergen al mismo flujo y llaman `visualizarDocumento()` con `DocumentResolveRequest`.

## Pendiente

- Tests E2E Playwright del flujo completo (click fila / menú) con fixtures/mocks del módulo.

