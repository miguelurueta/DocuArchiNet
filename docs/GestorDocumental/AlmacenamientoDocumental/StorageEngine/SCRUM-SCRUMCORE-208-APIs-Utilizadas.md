# SCRUMCORE-208 — APIs Utilizadas

## EmbedPDF (plugin-scroll)
- Hook: `useScroll(documentId)`
- State:
  - `scroll.state.currentPage`
  - `scroll.state.totalPages`
- Provides (acciones):
  - `scroll.provides?.scrollToPreviousPage()`
  - `scroll.provides?.scrollToNextPage()`

## Endpoints HTTP
- No aplica (este ticket no consume APIs HTTP nuevas).

