# SCRUMCORE-207 — Comportamiento del Componente

- Si el engine/documento no está listo: se mantienen estados `loading/error`.
- Si `provides` no existe para Print/Export: el handler no crashea (guard clause).

