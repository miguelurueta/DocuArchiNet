# SCRUMCORE-209 — APIs Utilizadas

## EmbedPDF DocumentManager (oficial)
Fuente: `@embedpdf/plugin-document-manager/react`

- `useDocumentManagerCapability()`
  - `provides.openDocumentUrl(options)`
  - `provides.retryDocument(documentId, options)`
  - `provides.onDocumentError(handler)`

## Modelos EmbedPDF
Fuente: `@embedpdf/models`

- `PdfErrorCode.Password` (código de error para password requerido/incorrecto).

## Endpoints HTTP
- No aplica: este ticket no agrega endpoints ni llamadas HTTP nuevas (se limita al visor local).

