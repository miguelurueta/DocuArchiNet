# SCRUMCORE-206 — Responsabilidades del componente

- Registrar `RotatePluginPackage` y consumir capabilities oficiales dentro de `AppVisorEmbedPdf`.
- Exponer acciones de rotación en el toolbar existente (presentacional, memoizado).
- Mantener Workbench/consumers sin lógica de plugins.

