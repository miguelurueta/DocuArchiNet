# SCRUMCORE-207 — Testing Enterprise

## Unit/Integration (Vitest + RTL)

- `AppVisorEmbedPdf.test.tsx`: valida click en Print/Export llama a `provides`.

## E2E (Playwright)

- TBD: smoke (click sin crash). El diálogo de impresión requiere estrategia de mocking.

