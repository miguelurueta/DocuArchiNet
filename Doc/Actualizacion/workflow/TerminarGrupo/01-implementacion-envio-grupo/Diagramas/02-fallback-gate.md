# Gate y fallback

```text
Gate existente activo   -> UI/ASMX modernos de grupo
Gate existente inactivo -> ImageButtonEnviaActividad y postback Web Forms legacy

Rollback autorizado -> gate inactivo + usuarios/grupos vacíos
```
