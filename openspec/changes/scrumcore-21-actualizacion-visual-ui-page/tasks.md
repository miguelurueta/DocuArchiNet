# Tasks

- [x] 1. Ajustar `.page` en `src/modules/gestionCorrespondencia/style/GestionCorrespondencia.module.css` para aplicar `gap: 2px`, `border-radius: 24px` y `background-color: #f5f5f5`.
- [x] 2. Alinear los breakpoints de `.page` para que el `gap` se mantenga consistente en desktop, tablet y mobile sin romper el layout.
- [x] 3. Actualizar `GestionCorrespondencia.tsx` para usar `variant="ghost"` en ambas acciones visibles y fijar `color: black` en `.variantGhost` de `AppButton.module.css`.
- [x] 4. Ejecutar las pruebas relevantes de `GestionCorrespondencia` y dejar evidencia del resultado.

> Evidencia:
> `npm test -- src/modules/gestionCorrespondencia/routes/GestionCorrespondenciaRoute.spec.test.tsx`
> Resultado: `1` archivo aprobado, `2` tests aprobados.
