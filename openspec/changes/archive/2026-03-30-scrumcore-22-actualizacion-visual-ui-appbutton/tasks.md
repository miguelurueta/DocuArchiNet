# Tasks

- [x] 1. Agregar en `GestionCorrespondencia.tsx` el botón `Actualizar` usando `AppButton`, `variant="ghost"`, `size="sm"` y `leftIcon={<UndoOutlined />}`.
- [x] 2. Crear un `handleRefresh` placeholder sin lógica de negocio real y mantener el orden visual coherente dentro del `AppToolbar`.
- [x] 3. Aplicar `background-color: white` a `.page` en `GestionCorrespondencia.module.css`.
- [x] 4. Ejecutar las pruebas relevantes de `GestionCorrespondencia` y dejar evidencia del resultado.

> Evidencia:
> `npm test -- src/modules/gestionCorrespondencia/routes/GestionCorrespondenciaRoute.spec.test.tsx`
> Resultado: `1` archivo aprobado, `2` tests aprobados.
