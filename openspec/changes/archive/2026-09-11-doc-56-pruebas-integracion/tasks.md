<!-- opsxj:refinement-traceability version=1 artifact=tasks decisions=D-01,D-02,D-03,D-04,D-05,D-06,D-07,D-08 -->
## 1. Suite transversal local

- [x] 1.1 [S] Crear la prueba de arquitectura que proteja rutas, dependencias y ausencia de arneses duplicados. Área/archivos: `Tests/importar-servicio-web-architecture.test.cjs`. Origen: D-01, RQ-01. Verificación: `node --test Tests/importar-servicio-web-architecture.test.cjs` finaliza en cero.
- [x] 1.2 [M] Crear la suite contractual para las ocho operaciones, estados y fixtures compartidos. Área/archivos: `Tests/importar-servicio-web-contract-suite.test.cjs`, `Tests/Fixtures/Workflow/ImportarServicioWeb/contracts-v1/`. Origen: D-04, RQ-04. Verificación: la suite valida cada operación contra su fixture y finaliza en cero.
- [x] 1.3 [M] Completar la matriz local de transporte, autorización, idempotencia, fallos y reconciliación reutilizando B01–B06. Área/archivos: `Tests/importar-servicio-web-*.test.cjs`. Origen: D-02, RQ-02. Verificación: `node --test Tests/importar-servicio-web-*.test.cjs` cubre éxito, rechazo, timeout, cancelación, reintento y fallo inyectado.

## 2. Compatibilidad e invariantes

- [x] 2.1 [S] Crear la regresión del gate para demostrar `FEATURE_DISABLED` sin efectos. Área/archivos: `Tests/importar-servicio-web-gate-regression.test.cjs`, `webservice/WebServiceImportarServicioWebModern.asmx.vb`. Origen: D-04, RQ-04. Verificación: la prueba demuestra retorno temprano y cero invocaciones downstream con gate apagado.
- [x] 2.2 [S] Crear la regresión legacy que proteja JavaScript y endpoints históricos. Área/archivos: `Tests/importar-servicio-web-legacy-regression.test.cjs`, `js/java_general/JSProgresBar.js`, `webservice/`. Origen: D-03, RQ-03. Verificación: la prueba falla si JavaScript inicia la ruta moderna o se sustituye una ruta legacy.
- [x] 2.3 [S] Crear la prueba de invariancia del almacenamiento. Área/archivos: `Tests/importar-servicio-web-storage-invariance.test.cjs`, `workflow/ClassAlmacenamiento.vb`, `Infrastructure/Workflow/ImportarServicioWeb/Storage/LegacyImportDocumentStorageAdapter.vb`. Origen: D-03, RQ-03. Verificación: falla si cambia el archivo legacy o aparece más de una invocación nueva a `AlmacenaDocumentoTareaWorkflow(...)`.

## 3. Validación automatizada

- [x] 3.0.1 [M] Implementar autorización de importación reutilizando exclusivamente el permiso 13 `ADJUNTAR_IMAGENES_PREDETERMINADA` comprobado por el flujo legacy, con tarea y contexto validados mediante consultas parametrizadas. Área/archivos: `Infrastructure/Workflow/ImportarServicioWeb/`, `webservice/WorkflowPreviewSessionContextGate.vb`. Origen: D-07, RQ-07. Verificación: pruebas demuestran rechazo sin permiso, tarea, ruta o trámite vigente.
- [x] 3.0.2 [M] Implementar resolución servidor de gabinete, ruta, clase documental y demás argumentos de infraestructura; definir `DocumentTypeId` como ID canónico de `tipo_doc_series`, validarlo junto con `DocumentTypeName` y traducirlo por trámite a un único `ID_TIPO_DOCUMENTAL_CHEQUEO` antes del almacenamiento legacy. Área/archivos: DTOs, repositorios y adaptadores de `ImportarServicioWeb`. Origen: D-07, RQ-07. Verificación: el request no acepta IDs de checklist ni gabinete/ruta/clase; cero, múltiples o nombres discordantes fallan cerrados y el comando final usa el ID contextual resuelto.
- [x] 3.0.3 [L] Implementar los pasos de descarga, preparación y almacenamiento secuencial con auditoría y reloj. Área/archivos: `Services/Workflow/ImportarServicioWeb/`, `Infrastructure/Workflow/ImportarServicioWeb/`. Origen: D-07, RQ-07. Verificación: prueba de integración local recorre fases y persiste transiciones sin modificar `ClassAlmacenamiento.vb`.
- [x] 3.0.4 [M] Publicar los cinco métodos ASMX faltantes con gate, contexto confiable, composición y errores seguros. Área/archivos: `webservice/WebServiceImportarServicioWebModern.asmx.vb`, proyecto VB. Origen: D-07, RQ-07. Verificación: prueba contractual encuentra ocho métodos y gate antes de toda dependencia.
- [x] 3.0.5 [M] Reemplazar la consulta genérica `items` por token y POST `consultarInformacionSello`, enviando el código de barras en el campo SII denominado `radicado` y aplanando `inscripciones[].imagenes[]` en items con clave opaca `codigoBarras + libro + registro + idanexo` sin exponer URL. Área/archivos: `Infrastructure/Workflow/ImportarServicioWeb/Sii/`, `DTOs/Workflow/ImportarServicioWeb/`. Origen: D-08, RQ-08. Verificación: prueba con servidor HTTP local exige los parámetros y límites del diccionario V1.01, comprueba códigos `0000`/`9998`/`9999` y cero, una y múltiples inscripciones/imágenes usando una respuesta saneada.
- [x] 3.0.6 [M] Resolver preview y descarga por la clave opaca, reconsultando `consultarInformacionSello` por código de barras y seleccionando exactamente la imagen por libro, registro e `idanexo` antes de validar y descargar su URL. La URL base SII y la descarga admiten HTTP y HTTPS por compatibilidad legacy; la descarga conserva validación DNS estricta contra el dominio configurado o sus subdominios reales y admite PDF, TIFF, PNG y JPEG declarados. Área/archivos: `Infrastructure/Workflow/ImportarServicioWeb/Sii/`, transporte HTTP común. Origen: D-08, RQ-08. Verificación: pruebas rechazan URL aportada por cliente, inscripción/anexo inexistente, dominios meramente parecidos, host fuera de allowlist, esquema diferente de HTTP/HTTPS, formato/tamaño inválido y demuestran descarga de bytes desde un servidor local controlado.
- [x] 3.0.7 [M] Implementar la matriz de metadatos SII para `mercantil`, `rup` y `esal`, separando `CODBARRAS` del recibo usado en `ENLASE`, `RECIBOCAJA` y radicado documental; normalizar prefijo de libro, fechas, longitudes y valores numéricos, usar `proponente` como matrícula RUP y escoger `DESCRIACTO` para Mercantil y `DESCRIPCIONA` para ESAL/RUP según los DDL reales. Preservar además los metadatos de inscripción/anexo definidos por el diccionario V1.01 para trazabilidad interna. Área/archivos: `Infrastructure/Workflow/ImportarServicioWeb/Storage/LegacyImportDocumentStorageAdapter.vb`, resolución servidor de metadatos SII, sin modificar `workflow/ClassAlmacenamiento.vb`. Origen: D-08, RQ-08. Verificación: pruebas parametrizadas por gabinete comparan cada nombre/valor con los DDL reales, registrando explícitamente la divergencia heredada de `PreAlmacenaConstanciaIsncripcionsSII` para RUP, rechazan desbordes o valores incompatibles y demuestran que archivo, usuario, expediente, TRD y clase documental siguen siendo derivados por el almacenamiento existente.
- [x] 3.0.8 [S] Proteger el contrato productivo SII y el hallazgo E2E con regresiones explícitas. Área/archivos: `Tests/importar-servicio-web-sii-*.test.cjs`, fixtures `sii-v1`, `tools/e2e/tests/importar-servicio-web-modern.spec.cjs`. Origen: D-08, RQ-08. Verificación: la suite falla si reaparecen `items`, `preview` o `resource`, si se confunden código de barras y recibo, o si la lectura E2E autorizada no encuentra al menos un anexo real para el radicado.

- [x] 3.0.9 [S] Crear en DocuArchi la persistencia append-only de intentos de servicios externos, vinculada por FK al catálogo `ra_ser_serviciointegracion` y correlacionada sin FK física con las intenciones Workflow. Área/archivos: `Doc/Actualizacion/workflow/ImportarServicioWeb/DOC-56-pruebas-backend-evidencia/Sql/003-create-ra-ser-intento-serviciointegracion.sql`, repositorio DocuArchi de integración. Origen: D-08, RQ-08. Verificación: una validación de esquema demuestra PK, FK local con borrado restringido e índices para disponibilidad, error, correlación e intención, sin crear dependencias referenciales entre bases de datos.
- [x] 3.0.10 [M] Normalizar en la frontera de cada proveedor los resultados técnicos y funcionales de consulta, token, preview, descarga y metadatos, y registrar exactamente un intento saneado por cada llamada externa exitosa o fallida. Área/archivos: `Infrastructure/Workflow/ImportarServicioWeb/Sii/`, contratos y repositorio de telemetría de servicios externos. Origen: D-08, RQ-08. Verificación: pruebas deterministas comprueban códigos y categorías estables, estado HTTP, reintentabilidad, duración, proveedor, operación y correlación; además demuestran que nunca se persisten credenciales, tokens, cookies, URLs con parámetros ni respuestas externas crudas.
- [x] 3.0.11 [M] Exponer consultas de disponibilidad basadas en intentos exitosos y fallidos sin acoplar el resultado funcional de la importación a la escritura de telemetría. Área/archivos: repositorio de lectura DocuArchi y `Tests/importar-servicio-web-sii-*.test.cjs`. Origen: D-08, RQ-08. Verificación: pruebas calculan total, éxitos, errores, porcentaje de disponibilidad y latencia por proveedor, operación y periodo, y confirman que un fallo al registrar telemetría no altera ni duplica la operación de negocio.

- [x] 3.0.12 [M] Proyectar la respuesta de `ExecuteImportIntent` con la misma confirmación documental conservadora de `GetImportIntent` y `ReconcileImportIntent`, completando `TaskId`, `DocumentId`, `DocumentName`, `ContentType` y `ReachedPhase` únicamente cuando el documento y su relación con la tarea estén confirmados; ocultarlos ante resultado incierto o inconsistente. Área/archivos: `Services/Workflow/ImportarServicioWeb/ImportServiceOrchestrator.vb`, `Services/Workflow/ImportarServicioWeb/ImportItemResultMapper.vb`, `Services/Workflow/ImportarServicioWeb/ServicioReconciliacionImportacion.vb`, composición de `webservice/WebServiceImportarServicioWebModern.asmx.vb`. Origen: D-07, RQ-07. Verificación: una prueba de servicio demuestra que una ejecución confirmada devuelve en `Items` todos los parámetros documentales necesarios para refrescar la interfaz y que ninguna ejecución no confirmada expone un `DocumentId` utilizable.
- [x] 3.0.13 [S] Blindar el contrato de resultado de ejecución que consume el frontend sin crear campos ni endpoints paralelos. Área/archivos: `Tests/importar-servicio-web-execution-contract.test.cjs`, `Tests/importar-servicio-web-document-list-contract.test.cjs`, fixtures contractuales y adaptador E2E de `tools/e2e/scripts/adapters/importar-servicio-web-e2e-adapter.cjs`. Origen: D-07, RQ-07. Verificación: pruebas contractuales validan `TaskId`, `DocumentId`, `DocumentName`, `ContentType`, `ReachedPhase`, `PersistenceKnown` y correlación en éxito, además de su supresión segura ante persistencia no confirmada.

- [x] 3.1 [M] Crear el runner local determinista sin autenticación ni red. Área/archivos: `tools/validation/Verify-ImportarServicioWebModern.ps1`. Origen: D-05, RQ-05. Verificación: ejecuta las suites afectadas, resume resultados y propaga un código distinto de cero ante fallos.
- [x] 3.2 [S] Validar estáticamente la configuración segura inicial y final. Área/archivos: `web.config`, configuración del gate moderno. Origen: D-06, RQ-06. Verificación: `WorkflowCentroTrabajoModernActive=false` y listas de usuarios y grupos vacías antes y después de toda corrida.

## 4. E2E real obligatorio y rendimiento SII

- [x] 4.1 [S] Registrar autorización explícita para ambiente, cuentas de prueba y datos/tareas descartables antes de autenticar o activar el gate. Área/archivos: bitácora saneada de ejecución DOC-56 y `tools/e2e/AGENT-RUNBOOK.md`. Origen: D-06, RQ-06. Verificación: existe referencia auditable de autorización sin credenciales; mientras falte, las tareas 4.3–4.6 permanecen bloqueadas y DOC-56 no puede cerrarse.
- [x] 4.2 [M] Implementar la especificación E2E real reutilizando exclusivamente autenticación, configuración, validadores y restauración de `tools/e2e`. Área/archivos: `tools/e2e/tests/importar-servicio-web-modern.spec.cjs`, soporte existente en `tools/e2e/`. Origen: D-06, RQ-06. Verificación: la especificación cubre autorización, lectura sin mutación, escritura autorizada, regresión legacy y restauración en `finally`, sin login ni `.env` paralelos.
- [x] 4.3 [M] Ejecutar el E2E real contra el ambiente y las cuentas expresamente autorizados. Área/archivos: `tools/e2e/tests/importar-servicio-web-modern.spec.cjs`, evidencia saneada DOC-56. Origen: D-06, RQ-06. Verificación: Playwright reporta resultados reales de cada caso, sin mocks ni simulación de respuestas SII, y conserva únicamente evidencia saneada.
- [x] 4.4 [M] Medir tiempos reales de cada operación de integración SII durante una muestra autorizada. Área/archivos: instrumentación de prueba en `tools/e2e/`, evidencia saneada DOC-56. Origen: D-06, RQ-06. Verificación: por operación se registran nombre, tamaño de muestra, éxitos/errores, mínimo, promedio, p50, p95, p99 y máximo en milisegundos, separando tiempo total y tiempo atribuible a SII cuando sea observable.
- [x] 4.5 [M] Ejecutar concurrencia controlada con un mínimo de dos solicitudes simultáneas y el nivel superior permitido por el perfil autorizado. Área/archivos: runner de concurrencia reutilizando `tools/e2e/`, datos descartables DOC-56. Origen: D-02, RQ-02. Verificación: registra concurrencia, duración, throughput, latencias p50/p95/p99, errores y correlaciones reales; demuestra que no se duplican intención ni documento.
- [x] 4.6 [M] Verificar estados y recuperación con resultados reales tras concurrencia, error y reintento autorizados. Área/archivos: endpoints modernos, consultas de control exclusivamente `SELECT`, evidencia saneada DOC-56. Origen: D-02, RQ-02. Verificación: cada correlación termina en el estado esperado, los reintentos permitidos/prohibidos se respetan y no hay mutaciones fuera del flujo probado.
- [x] 4.7 [S] Restaurar y comprobar la configuración segura aun cuando falle la corrida. Área/archivos: bloque `finally` del runner E2E y configuración del ambiente autorizado. Origen: D-06, RQ-06. Verificación: gate `false`, usuarios y grupos vacíos; una restauración fallida invalida la evidencia y bloquea el cierre.

## 5. Documentación y cierre

- [x] 5.1 [M] Crear el paquete técnico con documentos derivados del código, diagramas segmentados por responsabilidad y carpeta de evidencia saneada. Área/archivos: `Doc/Actualizacion/workflow/ImportarServicioWeb/DOC-56-pruebas-backend-evidencia/`. Origen: D-01, RQ-01. Verificación: existen `00-Indice.md` a `07-Metadata.md`; cada diagrama identifica símbolos, orden, decisiones, parámetros y tablas reales de frontera, SII, intención, ejecución, estados, reconciliación y telemetría; `Evidencias/` no contiene secretos ni respuestas externas crudas.
- [x] 5.2 [S] Publicar la matriz riesgo-requisito-prueba-evidencia con resultados locales y reales separados. Área/archivos: documentación DOC-56. Origen: D-06, RQ-06. Verificación: cada RQ referencia comando, fecha, ambiente saneado, resultado y artefacto; ningún caso omitido figura como aprobado.
- [x] 5.3 [S] Ejecutar validación local, OpenSpec y auditoría OPSXJ. Área/archivos: cambio `doc-56-pruebas-integracion` y documentación de evidencia. Origen: D-05, RQ-05. Verificación: los tres comandos finalizan en cero y los resultados quedan registrados.
- [x] 5.4 [S] Bloquear el cierre si falta cualquier E2E real, medición SII, prueba concurrente, restauración segura o evidencia exigida. Área/archivos: `tasks.md`, manifiesto OPSXJ y paquete documental DOC-56. Origen: D-06, RQ-06. Verificación: `opsxj:validate -- DOC-56` falla mientras cualquiera de las tareas 4.1–4.7 permanezca incompleta.
