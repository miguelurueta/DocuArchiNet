## 1. Contract

- [ ] 1.1 Agregar `suppressCellFocus` al contrato de `AppTable`.
- [ ] 1.2 Mantener `onRowClicked` y `rowSelection` como capacidades separadas.

## 2. Implementation

- [ ] 2.1 Configurar AG Grid para suprimir foco de celda por default.
- [ ] 2.2 Permitir override explícito cuando una pantalla necesite foco de celda.
- [ ] 2.3 Verificar que selección única y múltiple no se rompan.

## 3. Validation

- [ ] 3.1 Agregar pruebas para `onRowClicked`.
- [ ] 3.2 Agregar pruebas para selección única y múltiple.
- [ ] 3.3 Agregar pruebas para `suppressCellFocus=true` y `false`.
