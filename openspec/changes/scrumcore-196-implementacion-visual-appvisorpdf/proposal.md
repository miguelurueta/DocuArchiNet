## Why

IMPLEMENTACION-VISUAL-APPVISORPDF. Integración funcional de `AppVisorPdf` en el Tab **Documentos** con conexión a datos reales del módulo (listado/selección/carga), manteniendo responsive/overlay estable.

## What Changes

- Se genera automáticamente una propuesta OpenSpec basada en el issue SCRUMCORE-196.
- Se captura el resumen y la descripción del ticket como punto de partida.
- Se refina el alcance para que la integración sea funcional (negocio) y no solo visual.
- Se deja una base coherente para continuar con `design.md`, `spec.md` y `tasks.md`.

## Capabilities

### New Capabilities
- `implementacion-visual-appvisorpdf`: Integración funcional de `AppVisorPdf` en Tab Documentos (listado/selección/carga real) con responsive/overlay estable.

### Modified Capabilities
- 

## Impact

- Se agrega un flujo funcional de documentos en `gestionCorrespondencia` (Tab Documentos) para visualizar PDFs asociados a `idTareaWf`.
- Impacto detallado en `openspec/changes/scrumcore-196-implementacion-visual-appvisorpdf/design.md`, el spec y el `tasks.md`.

